naftiko: 1.0.0-alpha2
info:
  label: Bud Payments - Single
  description: Initiate, authorise, confirm, and retrieve a single Open Banking payment.
  tags:
  - Bud
  - Payments
  created: '2026-05-25'
  modified: '2026-05-25'
binds:
- namespace: env
  keys:
    BUD_ACCESS_TOKEN: BUD_ACCESS_TOKEN
capability:
  consumes:
  - type: http
    namespace: payments-single
    baseUri: https://api-sandbox.thisisbud.com
    description: Bud Payments - Single business capability. OAuth2 bearer-token authenticated against the Bud platform.
    resources:
    - name: initiate-single-payment
      path: /v1/payments/single
      operations:
      - name: initiate-single-payment
        method: POST
        description: Bud Payments - Single operation against Bud.
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: apikey
      key: Authorization
      value: 'Bearer {{env.BUD_ACCESS_TOKEN}}'
      placement: header
  exposes:
  - type: rest
    namespace: payments-single-rest
    port: 8080
    description: REST adapter for Bud Payments - Single. One Spectral-compliant resource per consumed operation.
    resources:
    - path: /v1/v1/payments/single
      name: initiate-single-payment
      description: REST surface for initiate-single-payment.
      operations:
      - method: POST
        name: initiate-single-payment
        description: Bud Payments - Single
        call: payments-single.initiate-single-payment
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: payments-single-mcp
    port: 9090
    transport: http
    description: MCP adapter for Bud Payments - Single. One tool per consumed operation.
    tools:
    - name: bud-initiate-single-payment
      description: Bud Payments - Single
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: payments-single.initiate-single-payment
      outputParameters:
      - type: object
        mapping: $.
